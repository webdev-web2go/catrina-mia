import SectionHeader from "@/components/section-header";

export default function Comments() {
  return (
    <section>
      <SectionHeader
        title="Descubre lo que otras personas opinan"
        subtitle="Comentarios"
        className="md:px-36"
      />
    </section>
  );
}
